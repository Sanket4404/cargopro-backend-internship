server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cargopro_db
    username: cargopro_user
    password: 2580
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
springdoc:
  swagger-ui:
    path: /swagger-ui.html
